<li>
	{{burger_name}}

	<button class="devour" data-id="{{id}}" data-newdevoured="{{devour}}">
		{{#if devour}}Devour Burger!{{else}}regurgitate{{/if}}
	</button>

		<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>
